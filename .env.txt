# Risk-free annual rate (decimal)
RISK_FREE_RATE=0.065
# Continuous dividend yield for underlying (decimal)
DIVIDEND_YIELD=0.00
# Your index symbol (to auto-detect spot rows if present in option CSVs)
INDEX_SYMBOL=NIFTY 50
# Timezone of timestamp strings in CSVs (IST or UTC); used only for display/consistency
CSV_TZ=Asia/Kolkata
